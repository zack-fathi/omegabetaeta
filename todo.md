The green confirmation crrently doesnt show when replies are sent. 

Remove the ccing from responses being sent, its unintuitive for obh to recieve its own reply. The email workflow should be intuitive and not cause confusion for the user or the organization. Currently i believe it works like: user sends email using send message, message is shown in messages and can be replied to. This reply shouldnt show up in the obh inbox. However their reply to the email should show in the inbox, which currently it does since its sent from the obh email. This allows obh to continue messaging from the regular email thread without confusion. Currently the most confusing thing is that the reply doesnt show the original message sent by the user, it should show the message and the reply below it, so when the user replies obh can see what the thread is about and respond accordingly. and the user can remember the thread too. 

Remove all "websitename says <warning>" default confirmation popups from the website buttons throughout the website and replace with a prettier version, currently it looks ugly. 


You have final discretion over how this should work and best practices, these are just general guidelines for the functionality I want to add. I want you to design the best system for this, and implement it in the best way possible.

update help docs to reflect changes